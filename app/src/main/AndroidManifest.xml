<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeUI"
        tools:targetApi="31">
        <activity
            android:name=".TextfieldButtonSnackActivity"
            android:exported="false"
            android:label="@string/title_activity_textfield_button_snack"
            android:theme="@style/Theme.ComposeUI" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/title_activity_state"
            android:theme="@style/Theme.ComposeUI" />
        <activity
            android:name=".ModifierActivity"
            android:exported="false"
            android:label="@string/title_activity_modifier"
            android:theme="@style/Theme.ComposeUI" />
        <activity
            android:name=".StateActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeUI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RowColumnActivity" />
    </application>

</manifest>